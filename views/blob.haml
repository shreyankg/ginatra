= partial(:header, :locals => {:repo => @repo})
%h3
  Blob: #{params[:splat].first}
.active
  .blob
    %h4
      %img{:src => prefix_url("img/doc.png")}/
      = @blob.name
    %pre
      %code= @blob.data
  .clearfix &nbsp;
