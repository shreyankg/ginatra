%ul.tabs
  %li
    %a.branch-graph{:href => "#{h prefix_url(@repo.param)}"} Source
  %li
    %a.branch-graph{:href => "#{h prefix_url(@repo.param)}#{h prefix_url(@repo.commits.first.id)}"} Commits
  %li
    = atom_feed_link(@repo.param, @repo.commits.first.id)
