%div{:class => "commit-#{role.to_s}"}
  %img{:src => gravatar_url(actor.email)}/
  %table{:cellpadding => "0"}
    %tr
      %td
        = actor.name
        %span.quiet
          (#{role.to_s})
    %tr
      %td= nicetime(date)
